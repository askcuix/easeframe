<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd"
    default-lazy-init="false">
    
    <description>Spring MVC Configuration</description>
    
    <!-- 扫描所有的controller -->
    <context:component-scan base-package="com.easeframe.demo.springmvc.web" />
    
    <!-- 注册DefaultAnnotationHandlerMapping和AnnotationMethodHandlerAdapter，将请求分发给@Controller -->
    <mvc:annotation-driven/>
    
    <!-- Handles HTTP GET requests for /css/** or /js/** by efficiently serving up static resources in the ${webappRoot}/css or ${webappRoot}/js directory -->
	<mvc:resources mapping="/favicon.ico" location="/favicon.ico" />
	<mvc:resources mapping="/css/**" location="/css/" />
	<mvc:resources mapping="/js/**" location="/js/" />
	<mvc:resources location="/images/" mapping="/images/**"/>  
    
    <!-- 直接将view返回给指定的url -->
    <mvc:view-controller path="/" view-name="index"/>
    <mvc:view-controller path="/login" view-name="login"/>
    <mvc:view-controller path="/unauthorized" view-name="common/403"/>

    <!-- 文件上传解析器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- the maximum file size in bytes -->
		<property name="maxUploadSize" value="1048576" />
	</bean>
	
	<!-- 国际化资源定义 -->
	<bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
		<property name="basename" value="i18n.applicationResources" />
		<property name="useCodeAsDefaultMessage" value="true" />
	</bean>

	<!-- View Resolver -->
	<bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="requestContextAttribute" value="rc" />
		<property name="prefix" value="/WEB-INF/pages/" />
		<property name="suffix" value=".jsp" />
	</bean>
    
</beans>